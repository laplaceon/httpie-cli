kind = TokenKind.TEXT
(int, TokenKind.NUMBER),
if not backslashes:
    (check_escaped_int, TokenKind.TEXT),
    for variation, kind in [
]:
        (int, TokenKind.NUMBER),
    try:
        (check_escaped_int, TokenKind.TEXT),
        value = variation(value)
    ]:
    except ValueError:
        try:
        continue
            value = variation(value)
    else:
        except ValueError:
        break
            continue
else:
    else:
kind = TokenKind.TEXT
    break
